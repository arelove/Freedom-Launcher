// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6a0dad',
    },
    secondary: {
      main: '#9c27b0',
    },
  },
});

export default theme;
